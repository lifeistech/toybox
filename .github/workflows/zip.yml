name: zip
on:
  pull_request:
    types:
      - closed
jobs:
    zip:
        if: github.event.pull_request.merged == true
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v2
          - run: sh ./.github/zip.sh
